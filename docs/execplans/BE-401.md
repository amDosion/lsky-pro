# BE-401 ExecPlan

## Goal

把 cycle-3 的 CLI-only intelligence backfill 升级为可调用的 control plane API，提供 preview / dispatch / status，且保持 admin-only 派发边界和小批量保护。

## Scope

- 新增 `ImageIntelligenceControlPlaneService`
- 新增 `Api\V1\IntelligenceController`
- `routes/web.php` 增加 control plane 路由
- 新增 focused feature test

## Constraints

- 不改正在并行推进的 Passkey / auth foundation
- 不回退 cycle-3 已落地的 backfill service
- 不把复杂控制逻辑塞回 `UserController`

## Validation

- `./vendor/bin/phpunit --filter=ImageIntelligenceControlPlaneTest`
- `./vendor/bin/phpunit --filter=AdvancedFeaturePagesTest`
- `php artisan route:list --path=advanced-api/intelligence`
