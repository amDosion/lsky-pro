# ExecPlan

## Ownership

- Task id: `BE-101`
- Owner role: `backend`
- Primary reviewer: `reviewer`

## Purpose / Big Picture

- 为 AI 配置和系统观测建立基础后端能力，避免后续 AI 与运维功能继续依赖静态配置和黑盒运行。

## Context and Orientation

- `AiProviderConfigService` 原先只有静态模型数组。
- 项目没有独立性能摘要接口。
- 批量删除服务已存在，但工作台没有接入预演/回滚 API。

## Plan of Work

1. 增加管理员态 AI 远端模型抓取接口。
2. 新增系统性能聚合服务和接口。
3. 补充测试覆盖和路由验证。

## Progress

- [x] AI 远端模型抓取接口完成
- [x] 系统性能接口完成
- [x] PHPUnit 和 route:list 验证完成

## Validation and Acceptance

- Commands:
  - `docker run --rm -v /opt/1panel/apps/lsky-pro:/var/www/html -w /var/www/html lsky-pro-custom:php83 ./vendor/bin/phpunit --filter=AdvancedFeaturePagesTest --testdox`
- Pass criteria:
  - 新增 API 可在路由清单中出现
  - `AdvancedFeaturePagesTest` 通过

## Outcomes and Retrospective

- 后端基础层已具备模型发现和性能摘要能力。
- AI intelligence 真正的任务/索引重构仍留待下一轮。
